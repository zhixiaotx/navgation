# 数据工具导出验证修复记录

数据工具导出已采用用户指定的本地验证方式：在当前浏览器输入 `admin / 123456` 后即可执行 JSON、浏览器 HTML、XLSX、CSV 与离线导航导出，不会跳转到外部账号登录页。验证成功状态保存在当前浏览器，直到用户点击数据工具中的“退出登录”。

实际界面复测表明，点击 JSON 导出会显示账号、密码、登录并导出与取消控件。填入约定凭据并提交后，验证状态已成功建立且弹窗关闭；重新打开数据工具可见“导出验证已通过”和“退出登录”。点击退出后，浏览器中的授权状态已清除，下一次导出需要重新验证。

项目保留原有外部账号登录，仅用于云端备份等需要服务器身份的功能；它不再影响数据工具的本地导出。
